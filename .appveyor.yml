version: 1.0.{build}

image: Visual Studio 2017
shallow_clone: true

matrix:
    fast_finish: true

platform:
    -x64

build_script:
    - C:\msys64\usr\bin\mintty.exe /bin/bash -cl "C:/projects/rotwkl/ci/build_win"

artifacts:
    - path: rotwkl.zip
